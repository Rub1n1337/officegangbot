name: Deploy to Railway

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    name: Lint & Syntax Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff

      - name: Run Ruff linter
        run: ruff check . --select E,F --ignore E501

      - name: Syntax check all Python files
        run: |
          find . -name "*.py" \
            -not -path "./.venv/*" \
            -not -path "./dashboard/*" \
            -exec python -m py_compile {} \;
          echo "✅ All Python files passed syntax check"

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Run pytest
        run: pytest -q

  deploy:
    name: Deploy to Railway
    runs-on: ubuntu-latest
    needs: [lint, test]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy to Railway
        # `up` uploads and builds the freshly checked-out commit, so the bot
        # always deploys the latest main. `redeploy` only re-ran the previous
        # deployment, which could leave the bot on stale code.
        run: railway up --service officegangbot --ci
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}